server:
  port: 8080

spring:
  application:
    name: api-gateway
  servlet:
    multipart:
      enabled: false

services:
  auth-url: http://localhost:8081
  tenant-url: http://localhost:8082
  project-url: http://localhost:8083

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000
