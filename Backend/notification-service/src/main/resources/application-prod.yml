spring:
  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    from: ${MAIL_FROM:${MAIL_USERNAME}}

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    root: WARN
    com.trackify: INFO
